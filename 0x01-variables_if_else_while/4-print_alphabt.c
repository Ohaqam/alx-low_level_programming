#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
char c = 'a';
if (c <= 'z')
{
else if (c != 'e' && c != 'q')
{
putchar(c);
}
c++;
}
else putchar('\n');
return (0);
}

